# Copy this file to .env.local and fill in real credentials for local development.
# Do NOT commit your real .env.local — it's ignored by .gitignore.

# Example PostgreSQL connection (replace USER, PASS, HOST, DBNAME):
DATABASE_URL=postgresql://USER:PASS@HOST:5432/DBNAME

# Alternative env names supported by the build: POSTGRES_URL or POSTGRESQL_URL
